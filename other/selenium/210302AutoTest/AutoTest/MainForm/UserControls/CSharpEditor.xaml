<UserControl x:Class="AutoTest.UserControls.CSharpEditor"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Button Grid.Row="1" Content="Compile" Name="btnCompile" Style="{StaticResource button}" Margin="5" HorizontalAlignment="Center"/>

            <avalonEdit:TextEditor Name="txtEditor" FontFamily="Consolas" FontSize="10pt" Padding="5"
                               ScrollViewer.HorizontalScrollBarVisibility="Auto" ScrollViewer.VerticalScrollBarVisibility="Auto">
            </avalonEdit:TextEditor>
        </Grid>
    </Grid>
</UserControl>
