package vn.neo.util;

public interface AppConstant {
    enum FindBy{
        Id,
        Name,
        TagName,
        ClassName,
        CssSelector,
        XPath,
        LinkText,
        PartialLinkText
    }
}
